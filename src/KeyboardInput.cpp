#include "KeyboardInput.hpp"
#include "Debug/Logger.h"
#include <GLFW/glfw3.h>

/* void Shadow::KeyboardInput::glfw_keyCallback(GLFWwindow *window, int key, int scancode, int
action, int mods) { if (key == GLFW_KEY_W && action == GLFW_PRESS) PRINT("Forward"); if (key ==
GLFW_KEY_A && action == GLFW_PRESS) PRINT("Left"); if (key == GLFW_KEY_S && action == GLFW_PRESS)
PRINT("Backward"); if (key == GLFW_KEY_D && action == GLFW_PRESS) PRINT("Right");

	if (key == GLFW_KEY_W && action == GLFW_RELEASE) PRINT("Forward Release");
	if (key == GLFW_KEY_A && action == GLFW_RELEASE) PRINT("Left Release");
	if (key == GLFW_KEY_S && action == GLFW_RELEASE) PRINT("Backward Release");
	if (key == GLFW_KEY_D && action == GLFW_RELEASE) PRINT("Right Release");
} */